The implementation plan is approved.

Implement only the work described in the assigned GitHub Issue.

Remain within scope.

After implementation:

- verify the application builds successfully,
- run all relevant verification steps,
- summarize the completed work,
- report any assumptions or limitations discovered.

Do not commit, push, create branches, merge Pull Requests, or modify Git history.